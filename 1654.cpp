#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    char s [20000];
    int i = -1;
    char temp;
    while((&temp = getchar ()) != '\n'){
        if (prev == "" && i == -1) prev = temp;
        else if (prev == "") prev = s[i];
        else if ()
            *p++ = temp;
        }
        if (prev != "" && temp == prev) {

        }

    };
    *p = '\0'; /* добавление нулевого символа */
    printf(s);
}
